version: "3.9"
services:


  flask-app:
    build: ./flask
    container_name: flask-app
    restart: always
    ports:
      - "5000:5000"

  nginx:
    build: ./nginx
    container_name: nginx
    restart: always
    ports:
        - "80:80"
    depends_on:
        - flask-app